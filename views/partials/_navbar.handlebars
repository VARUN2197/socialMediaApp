<nav class="navbar navbar-expand navbar-dark bg-dark fixed-top">
  <a class="navbar-brand" href="/">SocialMedia</a>
  {{#if user}}
      <a class="navbar-brand" href="/allUsers" id="allUsers">
      <i class="fa fa-users">All Users</i></a>
      <a class="navbar-brand" href="/allPost" id="posts">
      <i class="fa fa-globe">Posts</i></a>
  {{else}}
  {{/if}}
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
          <li class="nav-item">
             {{#if user}}
            <a class="nav-link" href='/logout'><i class="fa fa-sign-out"></i>Logout</a>
            {{/if}}
          </li>
    </ul>
  </div>
</nav>