<div class="container">
    <h1>Edit Post</h1>
    <form style="margin:40px" action="/editingPost/{{post._id}}?_method=PUT" method="POST">
    <div class="form-group">
        <label for="title">Title</label>
        <input name="title" class="form-control" placeholder="Enter Title" value={{post.title}}>
    </div>
    <div class="form-group">
        <label for="body">Body</label>
        <textarea name="postBody" type="text" class="form-control">{{post.body}}</textarea>
    </div>
    <div class="form-group row">
        <label for="status" class="col-sm-1 col-form-label">Status</label>
        <select class="form-control col-sm-2" name="status">
            <option value="public">Public</option>
            <option value="private">Private</option>
        </select>
    </div>
    <div class="form-group form-check">
        <input type="checkbox" name="allowComment" class="form-check-input" id="allowComment">
        <label class="form-check-label" for="allowComment">Allow Comment</label>
    </div>
    <button type="submit" class="btn btn-primary">Post</button>
    </form>
</div>